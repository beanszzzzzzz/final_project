{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "dockerfile"
    },
    "deploy": {
        "startCommand": "php bin/console doctrine:migrations:migrate -n --no-interaction && php -S 0.0.0.0:${PORT:-8000} -t public/"
    }
}